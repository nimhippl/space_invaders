{
  "name": "space-invaders",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "bun index.html --outdir=build --sourcemap=linked",
    "build": "bun build index.html --outdir=docs"
  },
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.3"
  }
}
